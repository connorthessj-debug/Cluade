# Python dependencies for the Raspberry Pi
# Install in a virtual environment: pip install -r requirements-pi.txt
#
# NOTE: dlib must be compiled from source on ARM (Pi).
# The setup_pi.sh script handles this automatically.

face_recognition==1.3.0
opencv-python-headless>=4.9.0
numpy>=1.24
requests>=2.31
Pillow>=10.0

# Optional: encrypt the face encoding database at rest
cryptography>=41.0

# Optional: for generating synthetic test data
scikit-learn>=1.3
